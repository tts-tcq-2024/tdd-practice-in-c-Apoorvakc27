#include<stdio.h>
#include<string.h>


int validate_string(const char *str)
{
    return 0;
}

int Addition_NumUsingString(const char *str)
{
    if(validate_string(str))
    {
        return 0;
    }
    return -1;
}

